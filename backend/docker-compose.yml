version: '3.8'

services:
  db:
    image: postgres:13-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: foodmanager_db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: